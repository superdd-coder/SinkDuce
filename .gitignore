# User data (mounted volume, never commit)
data/

# Dev scratch / local previews (not for commit)
frontend/preview.html
frontend/vite.preview.config.ts
frontend/src/main-preview.tsx
frontend/src/components/editorial/

# Environment config
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
.ruff_cache/
*.egg-info/
.eggs/
dist/
.venv/
venv/
.pytest_cache/
*.log
coverage/
htmlcov/
.coverage

# Local ONNX model packaging output (large binaries — publish via GitHub Release only)
# Layout: model_release/v<ver>/sinkduce-onnx-models-v<ver>.zip (+ sha256 / MANIFEST / notes)
model_release/

# Node
frontend/node_modules/
frontend/dist/

# Prototype (standalone testing project)
prototype/

# OS
.DS_Store

# IDE
.claude/
.qoder/
.vscode/
.idea/

# Playwright / Browser Console temp files
.playwright-cli/
.playwright-mcp/

# Superpowers session data
.superpowers/

# Agents skills cache
.agents/

# Cleanup plans
.cleanup_plan.md

# Agent skills lock files
skills-lock.json

# Project AI assistant configs
CLAUDE.md
AGENTS.md
development_doc/
design-demos/
.planning/
docs/
DESIGN_SYSTEM.html

# HyperFrames / local video renders (do not commit)
videos/

# Local contracts (enterprise IAM + frontend design). Not published with OSS.
contracts/

# Local macOS / Tauri packaging (never publish)
desktop/
src-tauri/
*.app
*.dmg

# ONNX weights: only the two in-tree bundles (RapidOCR + CAM++).
# Full ASR packs live in data/models/ (already ignored) or model_release/.
*.onnx
!src/parsers/ocr_models/*.onnx
!src/meeting/transcription/onnx/campplus_models/*.onnx

# ZCode agent local workspace
.zcode/
